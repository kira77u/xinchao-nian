name: Longing Push
"on":
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - name: Push to bark
        run: |
          curl -s "https://api.day.app/${{ secrets.BARK_KEY }}/想你了"
